/**

Enumération des types de requête pour la base de données.
Les types sont QUERY (requête de lecture) et UPDATE (requête de mise à jour).
*/

package types;


public enum QueryType {

    QUERY, UPDATE

}
